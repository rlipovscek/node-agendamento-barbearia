{% extends "_layout/default.njk" %}

{% block body %}
  <div class="content">
    <strong>Olá, {{ user.name }} </strong>
    <p>Inicie um agendamento escolhendo um profissional a baixo</p>
    <ul class="providers">
    {% for provider in provedores %}
      <li class="provider">
        <div>
          <img src="/files/{{provider.avatar}}" alt=""></img>
          <strong>{{ provider.name }}</strong>
        </div>
        <a href="/app/appointments/new/{{provider.id}}">+</a>
      </li>
    {% endfor %}
    </ul>
  <a href="/app/logout" class="logout">Sair</a>
  </div>

{% endblock %}